rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Extension data is stored at: users/{uid}/data/state
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
