{
  "scripts": {
    "setup:config": "node scripts/generate-config.js",
    "build:auth": "webpack",
    "build": "npm run setup:config && npm run build:auth",
    "package:extension": "bash ./scripts/package-extension.sh"
  },
  "devDependencies": {
    "webpack": "^5.103.0",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "firebase": "^12.6.0"
  }
}
